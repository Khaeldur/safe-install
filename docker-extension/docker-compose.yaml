services:
  safe-install-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9080:9080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - SAFE_INSTALL_CHANNEL=docker-extension
